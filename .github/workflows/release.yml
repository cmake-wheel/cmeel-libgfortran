name: Release on GitHub & PyPI

on:
  push:

env:
  CMEEL_JOBS: 2
  CMEEL_LOG_LEVEL: DEBUG

jobs:
  macos:
    name: "macos"
    runs-on: "macos-latest"
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: 'true'
      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - run: echo "OSX_SDK=$(xcrun --sdk macosx --show-sdk-path)" >> $GITHUB_ENV
      - run: python -m pip install -U pip
      - run: python -m pip wheel -vw wh .
        env:
          LDFLAGS: "-L${{ env.OSX_SDK }}/usr/lib"
          LIBRARY_PATH: "-L${{ env.OSX_SDK }}/usr/lib"
          CMEEL_CMAKE_ARGS: "-DSYSTEM_INCLUDEDIR=${{ env.OSX_SDK }}/usr/include"
      - uses: actions/upload-artifact@v3
        with:
          path: wh
